# Documentation History

This directory stores snapshots of cross-cutting documents that do not naturally belong under a single `docs/plans/<layer>/<module>/history/` directory.

- `progress/`: previous progress board formats and archived progress snapshots.
- `codex/`: previous agent charter snapshots.

Current working documents stay at their original paths, such as `docs/progress.md` and `docs/codex.md`.
