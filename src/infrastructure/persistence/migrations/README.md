# Persistence Migrations

Migration files for production database engines belong here. Module O keeps SQLite schema creation in repository adapters for local tests and early development, while canonical DDL assets live under `sql/ddl`.
